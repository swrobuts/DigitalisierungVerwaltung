
# Build outputs
# Cache (verschiedene Tools)
# Dependencies
# Editor / OS
# Local env (Vite-Convention: .env.*.local nie committen)
# Logs
# Testing
# lokales Tooling + Lehrmaterialien (kein Repo-Content)
*.log
*.pptx
*.swp
*.tsbuildinfo
.DS_Store
.cache/
.claude/
.env
.env.*.local
.env.local
.idea/
.superpowers/
.vite/
.vscode/
build/
coverage/
dist/
node_modules/
npm-debug.log*
# pnpm-lock.yaml — seit Phase 0.3 (pnpm-Workspace) wird das gebraucht für
# `pnpm install --frozen-lockfile` in GH Pages Workflow.
ue1/folien/*.pptx
